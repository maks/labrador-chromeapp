
module.exports.init = function(canvas) {
    var ctx = canvas.getContext("2d");
    var W = canvas.width;
    var H = canvas.height;
    var scale = 1;

    drawGrid(ctx);
}

function plotPoint(x, y) {

}

function drawGrid(context) {
    console.log("TODO: drawing grid");
}